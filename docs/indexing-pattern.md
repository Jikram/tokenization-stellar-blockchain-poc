# Indexing Pattern

The activity panel in the frontend is the first step toward event indexing.

A more mature architecture would add a Firehose/StreamingFast or transaction-listener component to aggregate events.

This document maps the demo to an indexing-ready pattern by treating Soroban events as first-class activity records.
